#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=update
#JWT_SECRET_KEY=${JWT_SECRET_KEY}
#JWT_ACCESS_TOKEN_EXPIRED_DATE=${JWT_ACCESS_TOKEN_EXPIRED_DATE}
#JWT_REFRESH_TOKEN_EXPIRED_DATE=${JWT_REFRESH_TOKEN_EXPIRED_DATE}
#spring.graphql.cors.allowed-origins=${ALLOWED_ORIGINS}
#firebase.bucket-name=${FIREBASE_BUCKET_NAME}
#firebase.generate-private-key-url=${FIREBASE_GENERATED_PRIVATE_KEY_URL}
#firebase.image-url=${FIREBASE_IMAGE_URL}
#spring.servlet.multipart.max-file-size=10MB
#spring.servlet.multipart.max-request-size=10MB
#logging.level.org.hibernate=ERROR

server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/fys_ecommerce?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL55Dialect
spring.jpa.hibernate.ddl-auto=update

# JWT
JWT_SECRET_KEY=mySecretKey123456789012345678901234567890
JWT_ACCESS_TOKEN_EXPIRED_DATE=86400000
JWT_REFRESH_TOKEN_EXPIRED_DATE=604800000

# CORS
spring.graphql.cors.allowed-origins=${ALLOWED_ORIGINS:*}

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Firebase (optional)
firebase.bucket-name=${FIREBASE_BUCKET_NAME:}
firebase.generate-private-key-url=${FIREBASE_GENERATED_PRIVATE_KEY_URL:}
firebase.image-url=${FIREBASE_IMAGE_URL:}

# cloudinary configurations
cloudinary.api.key=181157367961287
cloudinary.api.secret=WYH7xByx8WoTGO11a1_Tr0G2OJU
cloudinary.cloud.name=dyyoxj5fu

# Logging
logging.level.org.hibernate=ERROR
logging.level.org.springframework.security=DEBUG